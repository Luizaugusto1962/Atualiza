---
phase: 01-empacotamento
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - PKG-02
status: planned
must_haves:
  truths:
    - 'package.json does not declare a main pointing at a missing index.js.'
    - 'npm test still invokes npx bats -r tests/.'
    - 'Node is used only for dev tooling, not as the app runtime.'
  artifacts:
    - path: 'package.json'
      provides: 'Accurate project manifest for a Bash application'
      contains: '"test": "npx bats -r tests/"'
    - path: 'package.json'
      provides: 'No misleading main/type'
      not_contains: '"main": "index.js"'
  key_links:
    - from: 'package.json'
      to: 'tests/ (bats suite)'
      via: 'scripts.test -> npx bats -r tests/'
      pattern: 'npx bats -r tests/'
---

<objective>
Fix the misleading `package.json` `main` entry that points at a non-existent `index.js`, and remove the Node `"type": "commonjs"` declaration that misrepresents a pure-Bash project. Keep Node exclusively for dev tooling (`bats`, `prettier`). The `test` script must remain `npx bats -r tests/`.

Purpose: Fixes anti-pattern #2 — `package.json` declaring a Node entry point for a Bash application is misleading and can confuse tooling/consumers. Keeps the file accurate to the project's real (Bash) nature.

Output: `package.json` with no `main` pointing at a missing file; `scripts.test` unchanged and valid JSON.
</objective>

<execution_context>
@C:/Users/Luiz Augusto/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Luiz Augusto/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/Luiz Augusto/Atualiza/.planning/ROADMAP.md
@C:/Users/Luiz Augusto/Atualiza/.planning/REQUIREMENTS.md
@C:/Users/Luiz Augusto/Atualiza/.planning/codebase/ARCHITECTURE.md  (Anti-Patterns: optional index.js main in package.json)
@C:/Users/Luiz Augusto/Atualiza/.planning/codebase/STACK.md  (Node only for dev tooling)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the non-existent main/type entries from package.json</name>
  <read_first>package.json</read_first>
  <files>package.json</files>
  <action>
    Edit package.json to remove the `"main": "index.js"` line (line 5) and the `"type": "commonjs"` line (line 21). No `index.js` exists in the repo, and the app is Bash, so these fields are inaccurate. Do NOT add a `main` pointing at `atualiza.sh` unless you prefer an explicit entry — the acceptance tolerates either removal or pointing to `atualiza.sh`; choose removal for cleanliness.

    Keep ALL of the following intact (do not modify):
      - `"name": "atualiza"`, `"version": "1.0.0"`, `"description": ...`
      - `"directories": { "doc": "docs" }`
      - `"scripts": { "test": "npx bats -r tests/", "test:unit": "npx bats -r tests/unit", "test:integration": "npx bats -r tests/integration" }`
      - `"repository"`, `"keywords"`, `"author"`, `"license"`, `"bugs"`, `"homepage"`
      - `"devDependencies": { "bats": "^1.13.0", "prettier": "^3.9.5" }`

    After editing, ensure the JSON remains valid (correct comma placement — the line after the removed `main`/`type` must not leave a trailing comma; `description` is followed by `"directories"`, and `"homepage"` is followed by `"devDependencies"`).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); JSON.stringify(p); if('main'in p && p.main!=='atualiza.sh'){throw new Error('main still points at a missing file: '+p.main)} if('type'in p){throw new Error('type still present')} if(p.scripts.test!=='npx bats -r tests/'){throw new Error('test script changed')} console.log('PKG_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` check prints PKG_OK and exits 0.
    - `package.json` does NOT contain `"main": "index.js"` and does NOT contain `"type": "commonjs"`.
    - If `main` is present at all, its value is `atualiza.sh` (acceptable) — but removal is preferred.
    - `package.json` is valid JSON (parseable by `node -e "require('./package.json')"`).
    - `npm pkg get scripts.test` returns exactly `npx bats -r tests/`.
  </acceptance_criteria>
  <done>package.json no longer declares a non-existent `main`; the `test` script is preserved; JSON is valid.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm npm test still resolves the bats runner</name>
  <read_first>package.json</read_first>
  <files>package.json</files>
  <action>
    Verify the test wiring is intact without actually executing the full bats suite (which requires interactive/TTY-free environment and may be slow). Confirm the script string is exactly `npx bats -r tests/` and that `bats` is a declared devDependency so `npx bats` can resolve. Report the resolved script value.

    Run: `npm pkg get scripts.test` and confirm output equals `npx bats -r tests/`. Optionally sanity-check that `npx --no-install bats --version` resolves the pinned bats (this may download on first run; if offline, skip and rely on the devDependencies declaration).
  </action>
  <verify>
    <automated>npm pkg get scripts.test</automated>
  </verify>
  <acceptance_criteria>
    - `npm pkg get scripts.test` prints `npx bats -r tests/` (no leading/trailing whitespace differences).
    - `package.json` devDependencies still include `"bats": "^1.13.0"` and `"prettier": "^3.9.5"`.
    - No other `scripts` entries were altered (test:unit / test:integration unchanged).
  </acceptance_criteria>
  <done>`npm test` still maps to `npx bats -r tests/`; Node remains dev-tooling only; anti-pattern #2 removed.</done>
</task>

</tasks>

<must_haves>
  truths:
    - "package.json does not declare a `main` pointing at a missing index.js."
    - "`npm test` still invokes `npx bats -r tests/`."
    - "Node is used only for dev tooling, not as the app runtime."
  artifacts:
    - path: "package.json"
      provides: "Accurate project manifest for a Bash application"
      contains: "\"test\": \"npx bats -r tests/\""
    - path: "package.json"
      provides: "No misleading main/type"
      not_contains: "\"main\": \"index.js\""
  key_links:
    - from: "package.json"
      to: "tests/ (bats suite)"
      via: "scripts.test -> npx bats -r tests/"
      pattern: "npx bats -r tests/"
</must_haves>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest → tooling | `package.json` is read by npm and any consumer; a misleading `main` could misdirect Node-based importers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Spoofing/Info disclosure | `package.json` `main` field | accept | Risk is low: a Bash app has no Node importers; removing `main`/`type` only improves accuracy. No credentials, no network, no file-permission impact. Mitigated by deleting the misleading fields. |
| T-01-SC | Tampering | npm dev dependency installs | mitigate | No new packages added; `package-lock.json` already pins `bats@^1.13.0` and `prettier@^3.9.5`. Do not run `npm install` in a way that upgrades without the lockfile. |

Security baseline (`umask 077`, `0600`, config-injection defense, path-validation) is unaffected — this is a manifest-only change.
</threat_model>

<verification>
- `node -e` parse check prints PKG_OK.
- `grep '"main": "index.js"'` on package.json returns nothing.
- `npm pkg get scripts.test` returns `npx bats -r tests/`.
</verification>

<success_criteria>
PKG-02 satisfied: `package.json` no longer points `main` at a non-existent `index.js` (removed or pointed to atualiza.sh); `npm test` still runs `npx bats -r tests/`; Node remains dev-tooling only.
</success_criteria>

<output>
Create `.planning/01-empacotamento/01-02-SUMMARY.md` when done.
</output>
